const db = require('../db/models');

<% if (Backend === 'Express') { -%>
module.exports = (app) => {
    app.use((req, res, next) => {
        req.db = db;
<% } else if (Backend === 'Koa') { -%>
module.exports = (app) => {
    app.use((ctx, next) => {
        ctx.db = db;
<% } -%>
<% if (Backend === 'Express' || Backend === 'Koa' ) { -%>
        return next();
    });
};
<% } -%>